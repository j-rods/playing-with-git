#Git Cheatsheet

###Staging area


Add files to staging area:

` git add `

Add all files to staging area:

` git add -A `


###Basic Branching

Create a new branch:

` git branch branchname `

Switch to branch:

` git checkout branchname `
 

